{
    "version": 9,
    "configurePresets": [
        {
            "name": "env-base",
            "hidden": true,
            "cacheVariables": {
                "CMAKE_TOOLCHAIN_FILE": "$env{VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake"
            },
            // set env vars here
            "environment": {
                "VCPKG_ROOT": ""
            }
        }
    ]
}